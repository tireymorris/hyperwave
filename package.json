{
  "name": "hyperwave",
  "version": "1.0.50",
  "scripts": {
    "css": "unocss \"src/**/*.tsx\" -o public/styles/uno.css",
    "test": "bun run test",
    "dev": "bun css && bun --watch run src/server.tsx",
    "prettier": "bunx prettier --write src/ test/",
    "build": "bun css && bun build --compile ./src/server.tsx"
  },
  "dependencies": {
    "@unocss/cli": "^0.56.4",
    "hono": "^3.6.3"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "module": "src/server.js"
}
